<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	  TrollStore entitlements for the UTM-based host app.

	  Mirrors UTM's own Platform/iOS/iOS.entitlements (extended-virtual-
	  addressing + increased-memory-limit, needed for the VM's large address
	  space and RAM), plus dynamic-codesigning.

	  dynamic-codesigning is what makes QEMU's TCG take the fast W^X JIT path
	  instead of split-wx mirror mapping. In UTM's config that is the
	  hasJitEntitlements branch:
	      UTMQemuConfiguration+Arguments.swift:553  (see ARCHITECTURE.md)
	  TrollStore signs with it intact; a normal sideload cannot.
	-->
	<key>dynamic-codesigning</key>
	<true/>

	<key>com.apple.developer.kernel.extended-virtual-addressing</key>
	<true/>
	<key>com.apple.developer.kernel.increased-memory-limit</key>
	<true/>

	<key>get-task-allow</key>
	<true/>
</dict>
</plist>
