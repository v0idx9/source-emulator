# Build output
/build/
# Fetched third-party sources (populated by scripts/fetch-*.sh)
/third_party/
# macOS
.DS_Store
# Game content — never commit copyrighted binaries/assets
/game/
*.tipa
*.ipa

# Guest VM images and game content (never commit)
*.qcow2
*.img
*.raw
/vm/share/
/vm/*.utm/
